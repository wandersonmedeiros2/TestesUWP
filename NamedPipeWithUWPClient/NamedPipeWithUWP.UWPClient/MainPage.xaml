<Page
    x:Class="NamedPipeWithUWP.UWPClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:NamedPipeWithUWP.UWPClient"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="800"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.ColumnSpan="2"
            VerticalAlignment="Center"
            FontSize="30"
            Text="Named Pipe Client" />

        <StackPanel
            Grid.Row="1"
            Grid.ColumnSpan="1"
            Orientation="Horizontal">
            <TextBox
                x:Name="tbMessage"
                Width="500"
                Height="50"
                FontSize="30"
                Text="Message" />
            <Button
                x:Name="btSend"
                Width="200"
                Height="50"
                Click="BtSend_click"
                Content="Send"
                FontSize="30" />
        </StackPanel>

    </Grid>
</Page>
